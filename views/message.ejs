<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title><%= title %></title>
    <link rel="stylesheet" href="/styles.css" />
  </head>
  <body>
    <%- include("partials/header") %>
    <div class="main-container">
      <p>💬 <%= message.user %> says</p>

      <h1 class="message-text"><%= message.text %></h1>
      <small>Posted on <%= message.formattedDate %></small>

      <form
        action="/message/<%= message.id %>/delete"
        method="POST"
        style="margin-top: 0.4rem"
      >
        <button type="submit" class="delete-btn">Delete</button>
      </form>
      <div class="back-link">
        <a href="/">&leftarrow; Back to home</a>
      </div>
    </div>
    <%- include("partials/footer") %>
  </body>
</html>
