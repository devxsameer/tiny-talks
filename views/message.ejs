<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title><%= title %></title>
    <link rel="stylesheet" href="/styles.css" />
  </head>
  <body>
    <h1><%= message.user %> says 💬</h1>

    <p class="message-text"><%= message.text %></p>
    <small>Posted on <%= message.formattedDate %></small>

    <form
      action="/message/<%= message.id %>/delete"
      method="POST"
      style="margin-top: 1rem"
    >
      <button type="submit" class="delete-btn">🗑️ Delete</button>
    </form>

    <br /><br />
    <a href="/">⬅️ Back to all messages</a>
  </body>
</html>
